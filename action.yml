name: 'auto publisher'
author: 'K Sai Aditya'
description: 'Update apps in Microsoft Store'
inputs:
  command:
    description: 'The command to execute. Available command: `configure`, `get`, `update`, `poll`, or `publish`.'
    required: true
    default: 'configure'
  product-id:
    description: 'Product Id'
    required: true
  seller-id:
    description: 'Seller Id'
    required: true
  tenant-id:
    description: 'AAD Tenant Id'
    required: true
  client-id:
    description: 'App Client Id'
    required: true
  client-secret:
    description: 'App Client secret'
    required: true
  package-path:
    description: 'Path to the app package to upload'
    required: true
  json-file-path:
    description: 'Path to the JSON file containing app metadata'
    required: false
runs:
  using: 'node12'
  main: 'dist/main.js'
branding:
  icon: 'upload-cloud'
  color: 'blue'